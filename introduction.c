#include <stdio.h> // Imports the standard input output library

int main(){ // main function

    printf("Hello World!"); // prints output to terminal
    return 0; // terminate program, returns exit status
}
